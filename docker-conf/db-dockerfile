FROM mysql:latest
ENV MYSQL_ROOT_PASSWORD=asdf
EXPOSE 3306
ENTRYPOINT "mysql -u root -p asdf -e "CREATE DATABASE IF NOT EXISTS wpdb; GRANT ALL ON wpdb.* TO 'wpuser'@'localhost' IDENTIFIED BY 'asdf'; FLUSH PRIVILEGES ""
