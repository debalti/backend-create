FROM mysql:latest
ENV MYSQL_ROOT_PASSWORD=asdf
RUN $MYSQL -uroot -p$MYSQL_ROOT_PASSWORD -e "CREATE DATABASE IF NOT EXISTS wpdb; GRANT ALL ON wpdb.* TO 'wpuser'@'localhost' IDENTIFIED BY 'asdf'; FLUSH PRIVILEGES "
EXPOSE 3306
